diff --git a/docs/docs/Lyrics_Next/v2.1.md b/docs/docs/Lyrics_Next/v2.2.md
index 8c6af86..653989c 100644
--- a/docs/docs/Lyrics_Next/v2.1.md
+++ b/docs/docs/Lyrics_Next/v2.2.md
@@ -1,4 +1,4 @@
-# Lyrics Next 格式规范第二版 第一次修正
+# Lyrics Next 格式规范第二版 第二次修正
 
 > [!note]
 > 仅供娱乐，当然如果你的软件想使用这个格式是完全可以的（真的会有人用吗？）
@@ -41,19 +41,17 @@ Lyrics Next 歌词文件可以直接使用 `lrc` 后缀，也可以使用单独
 
 歌词头部可选的包含歌词相关信息，如：
 
-| 标签                           | 含义                                                     |
-| ------------------------------ | -------------------------------------------------------- |
-| [Lyrics Next]                  | Lyrics Next 格式                                         |
-| [version:2.1]                  | 格式版本                                                 |
-| [timing:syllable]              | 项目类型<br>`line` 为逐行，`word` `syllable` 为逐字/节拍   |
-| [lang:zh-CN]                   | 主要语言                                                 |
-| [translates.lang:en-US]        | 翻译语言                                                 |
-| [transliterations.lang:pinyin] | 发音语言                                                 |
-| [songwriter:HOYO-MiX]          | 制作人名称                                               |
-| [agent.v1:person]              | `agent` 对应信息                                         |
-| [agent.v2:group]               | 同上                                                     |
-| [by:MiaowCham]                 | 歌词制作者                                               |
-| [offset:0]                     | 歌曲偏移量（单位毫秒）                                   |
+| 标签                  | 含义                                                     |
+| --------------------- | -------------------------------------------------------- |
+| [Lyrics Next]         | Lyrics Next 格式                                         |
+| [version:2.2]         | 格式版本                                                 |
+| [timing:syllable]     | 项目类型<br>`line` 为逐行，`word` `syllable` 为逐字/节拍 |
+| [lang:zh-CN]          | 主要语言                                                 |
+| [songwriter:HOYO-MiX] | 制作人名称                                               |
+| [agent.v1:person]     | `agent` 对应信息                                         |
+| [agent.v2:group]      | 同上                                                     |
+| [by:MiaowCham]        | 歌词制作者                                               |
+| [offset:0]            | 歌曲偏移量（单位毫秒）                                   |
 
 若一个标签有多个内容，可以用 `,` 分割或者使用多个标签表示
 
@@ -90,21 +88,24 @@ Lyrics Next 歌词的标准格式为：
 
 #### 行标签中，*Lyrics Next 第二版* 额外扩展了 `end`、`agent` 和 `line` 标签，允许进行以下类型的省略：
 
-- `[start,end,line]`（只省略 `agent` 标签，单一演唱者时可以使用）
+- `[start,end,,line]`（只省略 `agent` 标签，单一演唱者时可以使用）
 - `[start,,agent,line]`（只省略 `end` 标签）
-- 同理可以省略 `line` 标签，但不建议省略，可能影响翻译和发音的解析
+- 同理可以省略 `line` 标签 (带着逗号一起)，但不建议省略，可能影响翻译和发音的解析  
+  `[start,end,agent]` `[start,end]` `[start,,agent]`
 - `[start]`（全部省略，LRC 格式）
 
-为了保持格式完整，只省略 `end` 标签时，必须保留逗号。  
+为了保持格式完整，只省略 `end` 和/或 `agent` 标签时，必须保留逗号。  
 
-省略 `end` 标签时，解析应当遵循：
+省略 `end` 标签时，解析应当遵循:
 
 - 有按节拍划分的，将本行最后一个节拍的结束时间作为结束时间
 - 逐行的，以下一行的开始时间作为结束时间
 
 #### 按节拍划分标签中，*Lyrics Next 第二版* 额外扩展了 `end` 标签，允许省略：
 
-- `<start>`（全部省略，增强型LRC 格式）
+- `<start>`（全部省略，增强型 LRC 格式）
+
+一行首个节拍可以省略按节拍划分标签，则该节拍的开始时间为行标签的开始时间，结束时间为下一节拍的开始时间 (兼容增强型 LRC)
 
 ### 逐句歌词  
 
@@ -160,6 +161,9 @@ Lyrics Next 歌词的标准格式为：
 
 是一种基于行 ID 进行精准对应歌词的格式，文件可以直接使用 `lrc` 后缀，也可以使用单独的 `lnt` 后缀
 
+> [!Warning]
+> 若主歌词省略了 `line` 标签，则 ***不允许*** 使用 LRCN Trans 格式
+
 支持以下格式：
 
 ```LRCN Trans
@@ -177,7 +181,7 @@ Lyrics Next 歌词的标准格式为：
 
 ### 翻译及行发音
 
-内嵌：在包含多份歌词内嵌时，支持以下格式进行标记不同的歌词文件：
+#### 内嵌：在包含多份歌词内嵌时，支持以下格式进行标记不同的歌词文件：
 
 ```lrcn
 [type: format@xxx]
@@ -192,7 +196,7 @@ Lyrics Next 歌词的标准格式为：
 ```
 # 头部信息
 [Lyrics Next]
-[version:2.1]
+[version:2.2]
 ...
 
 # 主歌词
@@ -216,7 +220,10 @@ Lyrics Next 歌词的标准格式为：
 ...
 ```
 
-外置：为了满足 i18n 需求，建议尽可能使用外置翻译/发音。  
+> [!note]
+> 但凡内嵌翻译和(或)发音的歌词文件，必须进行格式标记。纯 LRCN 可不包含格式标记
+
+#### 外置：为了满足 i18n 需求，建议尽可能使用外置翻译/发音。  
 外置翻译/发音文件应当是 LRC 或 LRCN Trans 格式文件，具体格式如下：
 
 ```lrc
